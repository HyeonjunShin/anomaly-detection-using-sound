{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "clear_and_run",
      "command": "cls || clear && uv run python ${file}",
      "type": "shell",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
